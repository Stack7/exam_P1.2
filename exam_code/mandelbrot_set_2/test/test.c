#include <stdio.h>
#include <stdlib.h>
#include <complex.h> 
//calculate the modulus for a complex number

#include "zpoint.h"
#define DOUBLE_PRECISION

#ifdef _OPENMP
printf("I activate the OPENMP paradigm! Have fun!"\n)
#endif
int main (int argc, int* argv[])
{
    short int I=0;

   nx = argv[1]; 
   ny = argv[2];
   xL =a rgv[3];
   yL = argv[4];
   xR = argv[5];
   yR = argv[6];
   I_max= argv[7];




   // while(i!=I_max && cabs(z)=>2){


 // i++;  }

    

    return 0;
}